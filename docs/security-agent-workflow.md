# Security-Agent Targeting Workflow

## Path Contract

There are always two different paths:

- `SECURITY_AGENT_HOME`: this repository root (`${SECURITY_AGENT_HOME}` at runtime)
- `TARGET_REPO`: the repository being analyzed, passed explicitly as `--repo <path>`

OpenCode runs from `SECURITY_AGENT_HOME`. The security-agent CLI operates on `TARGET_REPO`.

Never read the target repository source tree directly into chat context during recon. Recon must produce intermediary artifacts first.

Filesystem boundary:
- Allowed roots: `SECURITY_AGENT_HOME` and `/tmp`.
- Forbidden: the global Ghost home and arbitrary folders outside the VulnOps workspace.
- Ghost evidence must be generated under `scans/<reponame>/evidence/ghost/` before native import.

## Step-By-Step Flow

1. Operator starts OpenCode in `SECURITY_AGENT_HOME`.

   ```bash
   cd <vulnops-root>
   export SECURITY_AGENT_HOME="$PWD"
   bins/shims/opencode
   ```

2. Operator initializes the target repository workspace.

   ```bash
   node --experimental-strip-types ./src/cli.ts init --repo "$TARGET_REPO"
   ```

   Outputs:
   - `scans/<reponame>/config/project.json`
   - `scans/<reponame>/config/target.json`
   - `scans/<reponame>/config/tools.json`
   - `scans/<reponame>/evidence/agent-harness-kit.json`

   Harness behavior:
   - OpenCode connects the `agent-harness-kit` MCP server from `opencode.jsonc`.
   - The CLI writes phase/gate/action/file/tool/acceptance state directly into `.harness/harness.db`, the same SQLite store read by AHK MCP and `ahk status --json`.
   - AHK root tasks are seeded from the workflow contract and `.harness/feature_list.json` with `bins/shims/ahk sync --direction in`.
   - AHK CLI validation must run sequentially; do not run `ahk sync` and `ahk status` in parallel against the same SQLite DB.
   - Operator chat is not outside the process: target workflow requests must use the harness before work starts.
   - External tools are tracked as individual harness gates: codeTree, GitNexus, Semble, Ghost, OpenGrep, and Cognium.
   - Acceptance criteria must be marked met before task completion; `done` with unmet acceptance is downgraded to `blocked`.

3. Containment doctor runs before external evidence is trusted.

   ```bash
   node --experimental-strip-types ./src/cli.ts doctor --repo "$TARGET_REPO"
   node --experimental-strip-types ./src/cli.ts toolchain verify
   ```

   The doctor checks vendored Ghost/OpenCode binaries, shim-backed MCP commands, patched Ghost skill paths, and forbidden global/user-space path references. `toolchain verify` writes `toolchain.lock.json` and marks missing contained tools as `not_portable_blocker`.

4. Mandatory coverage gates run before recon.

   ```bash
   node --experimental-strip-types ./src/cli.ts run --repo "$TARGET_REPO" --stages recon,discovery,triage
   ```

   Default mode is strict: blocked required gates stop the run and write `scan_status: coverage_incomplete` to `scans/<reponame>/reports/mvp-summary.md`. Use `--allow-degraded` only for research runs.

5. Safe Ghost workflows run against the target before native import.

   OpenCode skill calls:
   - `ghost-repo-context`
   - `ghost-scan-deps`
   - `ghost-scan-secrets`
   - `ghost-scan-code`
   - `ghost-report`

   Forbidden:
   - `ghost-validate`
   - `ghost-proxy`
   - live validation
   - traffic capture

   Outputs are generated by Ghost under `scans/<reponame>/evidence/ghost/` and then imported under canonical `scans/<reponame>/` findings and reports. If a Ghost skill cannot run, the complete scan is `coverage_incomplete` unless `--allow-degraded` is explicit.

6. Recon prepares supporting tools before source review.

   ```bash
   node --experimental-strip-types ./src/cli.ts recon --repo "$TARGET_REPO" --prepare-tools
   ```

   Supporting tool behavior:
    - codeTree is started by the CLI recon stage with `--root TARGET_REPO` (not called directly from the OpenCode session). The session's codeTree MCP server is scoped to `SECURITY_AGENT_HOME`; agents must read `scans/<reponame>/evidence/graph/codetree-structure.json` instead of calling codeTree tools directly.
    - GitNexus indexes the target and runs a security-focused graph query.
   - Semble runs focused retrieval searches for entrypoints, authz, data access, shell/file/crypto/secret surfaces.
   - CLI codeTree detection remains a fallback artifact when MCP structure is unavailable.
   - Supporting tools are time-budgeted, but complete scans require gate success before this phase.
   - Ghost repo context generated in the prior OpenCode step is imported by default.

   Harness-gated intermediary artifacts:
   - codeTree: `scans/<reponame>/evidence/graph/codetree-structure.json` or blocker.
   - GitNexus: `scans/<reponame>/evidence/graph/gitnexus-analyze.json` and `gitnexus-query.json` or blocker.
   - Semble: `scans/<reponame>/evidence/graph/semble-searches.json` or blocker.

   Outputs:
   - `scans/<reponame>/kb/supporting-tools.json`
   - `scans/<reponame>/evidence/graph/gitnexus-analyze.json`
   - `scans/<reponame>/evidence/graph/gitnexus-query.json`
   - `scans/<reponame>/evidence/graph/semble-searches.json`

7. Recon builds the canonical knowledge base.

   Outputs:
   - `scans/<reponame>/kb/repo-map.json`
   - `scans/<reponame>/kb/languages.json`
   - `scans/<reponame>/kb/dependencies.json`
   - `scans/<reponame>/kb/entrypoints.json`
   - `scans/<reponame>/kb/callgraph.json`
   - `scans/<reponame>/kb/dataflows.json`
   - `scans/<reponame>/kb/threat-model.md`
   - `scans/<reponame>/kb/ghost-context.json`
   - `scans/<reponame>/integrations/ghost/skills.json`
   - `scans/<reponame>/reports/recon.md`

8. Discovery reads KB artifacts and tool evidence, then scans.

   ```bash
   node --experimental-strip-types ./src/cli.ts discovery --repo "$TARGET_REPO"
   ```

   Outputs:
   - `scans/<reponame>/findings/raw/*.json`
   - `scans/<reponame>/findings/normalized/findings.json`
   - `scans/<reponame>/findings/normalized/ghost-code-findings.json`
   - `scans/<reponame>/findings/normalized/ghost-deps-findings.json`
   - `scans/<reponame>/findings/normalized/ghost-secrets-findings.json`
   - `scans/<reponame>/reports/discovery.md`

   Semantic SAST behavior:
   - Cognium runs when installed: `cognium scan ./src --category security --exclude-tests --format json`.
   - If `$TARGET_REPO/src` does not exist, the adapter scans `.` and records that limitation.
   - Cognium exit code `1` is accepted because its CLI uses it when security findings are present.
   - OpenGrep and Cognium have separate harness tasks and cannot be marked done until their raw artifacts exist.
   - Sensitive exposure discovery covers logs, telemetry, debug output, response/error output, and config dumps without targeting one known issue.

9. Triage reads normalized findings and KB artifacts.

   ```bash
   node --experimental-strip-types ./src/cli.ts triage --repo "$TARGET_REPO"
   ```

   Outputs:
   - `scans/<reponame>/findings/triaged/findings.json`
   - `scans/<reponame>/reports/triage.md`
   - Ghost/native reconciliation fields in triaged findings.

10. Rescore auto-runs after triage to re-evaluate needs-human-review findings.

   The rescore stage applies richer KB context to findings the primary triage couldn't resolve:
   - Ghost-verified findings are accepted at claimed severity
   - Scanner noise is rejected
   - Test/example/doc path findings without evidence are rejected
   - Same-directory entrypoint proximity upgrades reachability
   - Dependency findings with CVE/GHSA evidence are accepted
   - Secrets in production code paths are accepted

   Outputs:
   - `scans/<reponame>/reports/human-review-rescore.md`

11. Report produces the operator handoff.

   ```bash
   node --experimental-strip-types ./src/cli.ts report --repo "$TARGET_REPO"
   ```

   Outputs:
    - `scans/<reponame>/reports/mvp-summary.md`
    - `scans/<reponame>/reports/detailed-report.md`
    - `scans/<reponame>/reports/ghost-summary.md`

## Agent Context Rules

Agents may read:
- `config/target.json`
- `kb/*.json`
- `evidence/graph/*.json`
- `findings/normalized/findings.json`
- `findings/triaged/findings.json`
- `reports/*.md`

Agents must not bulk-read:
- whole source files
- whole directories
- raw scanner logs
- dependency lockfiles unless a specific finding requires it

When source context is needed, agents should request targeted snippets using artifact references: file path plus line number.
